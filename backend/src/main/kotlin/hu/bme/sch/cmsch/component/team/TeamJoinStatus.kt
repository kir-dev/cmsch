package hu.bme.sch.cmsch.component.team

enum class TeamJoinStatus {
    INSUFFICIENT_PERMISSIONS,
    ALREADY_IN_GROUP,
    JOINING_DISABLED,
    GROUP_NOT_FOUND,
    NOT_JOINABLE,
    ALREADY_SUBMITTED_JOIN_REQUEST,
    OK
}
